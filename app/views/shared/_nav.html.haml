.navbar.navbar-fixed-top
  .navbar-inner
    .container
      =link_to "Backlog", root_path, {:class => 'brand'}
      %ul.nav
        %li= link_to "Projects", projects_path
        %li.dropdown
          =link_to nil, {:class => 'dropdown-toggle', :'data-toggle' => 'dropdown', :'data-target' => "#"} do
            %i.icon-calendar.icon-white
            New Project Action
            %b.caret
          %ul.dropdown-menu
            - Project.all.each do |project|
              %li=link_to project.name, project

